language: generic 

services:
  - docker

jobs:
  include:
    - stage: compile
      script:
        - exec docker run -a STDOUT -a STDERR --rm -v $(pwd):/ws/src/marti_common ros:dashing /ws/src/marti_common/.ros2_build.sh
